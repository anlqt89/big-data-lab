FROM node:20-alpine

WORKDIR /app

# Install dependencies first (better caching)
COPY package*.json ./
RUN npm ci --omit=dev

# Copy source
COPY . .

# Start command (must match your package.json "start" script)
CMD ["npm","start"]
